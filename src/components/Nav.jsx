import React from "react";

// Nav is now embedded directly in Header.jsx as a transparent overlay.
// This file is kept as a re-export for backward compatibility if needed.

const Nav = () => null;

export default Nav;
